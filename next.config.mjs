/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["hrms.wu.ac.th"],
  },
  // experimental: {
  //   middleware: true,
  // },
  images: {
    domains: ["hrms.wu.ac.th"],
  },
};

export default nextConfig;
